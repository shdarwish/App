<!-- the form to add and edit users -->
<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field text-left">
    <%= f.label :name, class: 'form-group' %><br>
    <%= f.text_field :name, class: 'appformcontrol form-control' %>
  </div>
  <div class="field text-left">
    <%= f.label :password,  class: 'form-group' %><br>
    <%= f.password_field :password, class: 'appformcontrol form-control' %>
  </div>
  <div class="field text-left">
    <%= f.label :password_confirmation, class: 'form-group'  %><br>
    <%= f.password_field :password_confirmation, class: 'appformcontrol form-control' %>
  </div>
  <div class="field text-left">

    <%= f.check_box :admin %>Admin 
 &nbsp; &nbsp;<%= f.label :admin, class: 'admincheck form-group' do %>
  <% end %>
    
    </div>
    <div class="actions">
      <%= f.submit class: 'userformbutton btn btn-info' %>
      <%= link_to 'Back', users_path, class: 'userformbutton btn btn-info userbackbutton'%>
    </div>

<% end %>